module.exports = {
    content: ['./index.html', './src/**/*.{js,jsx}'],
    darkMode: 'media',
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [
        require('daisyui'),
    ],
}